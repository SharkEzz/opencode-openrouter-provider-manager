Hotkey chip — trailing indicator in palette headers, command rows and tooltips.

```jsx
<Kbd>ctrl+p</Kbd>
<Kbd tone="accent">⏎</Kbd>
```

Always lowercase, always mono, never wider than the key text. Combinations use `+` with no spaces.
