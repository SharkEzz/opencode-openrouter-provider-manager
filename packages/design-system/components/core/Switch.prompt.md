Compact 14px-track toggle — fallback providers, pricing tiers, inspector flags.

```jsx
<Switch checked={fallback} onChange={setFallback} label="auto-fallback" hint="retry on 429 via secondary provider" />
```

On: electric blue track with a white thumb and blue glow. Off: raised pane fill, slate thumb. Never stretch the track — density is fixed.
