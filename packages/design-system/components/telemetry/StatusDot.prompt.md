Health dot for providers, MCP servers and uptime readouts.

```jsx
<StatusDot tone="ok" ping label="uptime 99.98%" />
```

Only one `ping` dot should be visible in a viewport at a time — it means "streaming right now". Static dots for everything else.
